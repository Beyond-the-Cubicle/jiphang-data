spring:
  datasource:
    driver-class-name: com.mysql.cj.jdbc.Driver
    url: jdbc:mysql://${DB_URL:localhost:3306/route_information}?allowMultiQueries=true&useSSL=false&useUnicode=yes&characterEncoding=UTF-8&characterSetResults=UTF-8
    username: ${DB_USER:root}
    password: ${DB_PASSWORD:**0xKkwan}
  jpa:
    hibernate:
      ddl-auto: create

logging:
  level:
    root: info

data-portal:
  service-key: data-portal-service-key
data-dream:
  service-key: data-dream-service-key
